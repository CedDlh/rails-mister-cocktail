<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.input :description, label: "Amount" %>
  <%= f.association :ingredient %>
  <%= f.submit "Let's Add", class: "btn btn-primary btn-lg" %>
<% end %>
